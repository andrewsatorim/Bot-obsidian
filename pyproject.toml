[project]
name = "bot-obsidian"
version = "0.1.0"
description = "Multi-Layer Crypto Decision Engine"
requires-python = ">=3.11"

[tool.black]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
